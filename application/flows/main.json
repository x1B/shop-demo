{
   "redirectOn": {
      "unknownPlace": "error404"
   },
   "places": {
      "error404": {
         "patterns": [ "/not-found" ],
         "page": "404"
      },

      "browse": {
         "patterns": [ "/browse/:search(.*)", "/browse" ],
         "targets": {
            "next": "confirmation"
         },
         "page":  "shop_demo"
      },

      "": {
         "redirectTo": "browse",
         "page":  "shop_demo"
      },

      "confirmation": {
         "patterns": [ "/confirm" ],
         "page":  "finish_order"
      }
   }
}
